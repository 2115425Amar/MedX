<!-- filepath: /home/hp/Downloads/medicalPortal/app/views/dashboard/index.html.erb -->

 <nav class="flex justify-center md:py-2 mx-4">
         <div class="flex w-full md:px-6 justify-between items-center">
             <%= link_to posts_path do %>
            <div class="flex gap-4 p-3 items-center">
               <p class="text-2xl md:text-3xl font-serif font-semibold text-gray-600">Dashboard-index</p>
            </div>
            <% end %>
            <div class="flex gap-4 p-2 items-center">
               <%= image_tag current_user.avatar_url, class: "w-10 h-10 rounded-full" %>
               <div class="flex flex-col">
                  <h2 class="text-lg mb-1"><%= current_user.first_name %> <%= current_user.last_name %></h2>
                     <div class="text-sm">
                      <%= current_user.has_role?(:doctor) ? "Doctor" : "Receptionist" %>
                    </div>
               </div>
            </div>
         </div>
      </nav>
<div class="p-6">

<% if current_user.doctor? %>
  <p>Welcome Doctor <%= current_user.name %>!</p>
<% end %>

  <!-- Users Registered Over Time -->
  <div class="mb-6">
    <h2 class="text-xl font-semibold mb-2">Users Registered Over Time</h2>
    <%# line_chart @chart_data, height: "300px" , xtitle: "Date", ytitle: "Users" %>
    <%= line_chart @chart_data, height: "300px", xtitle: "Date & Time", ytitle: "Users" %>

  </div>

  <!-- Add more sections as needed -->
</div>